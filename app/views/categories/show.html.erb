<h2 class="title is-2"><%= @category.name %></h2>

<p class="mb-5">
  This category has  <%= @category.products.count %> of the items for sale.
</p>

<h3 class="title is-3">Products part of this category: <%= @category.name %></h3>
<h4>Click on a product to learn more</h4>
<ul>
  <% @category.products.each do |product| %>
    <li><%= link_to product.name, product %></li>
  <% end %>
</ul>